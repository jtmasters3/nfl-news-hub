{
  "publishing_mode": "approval",
  "note": "publishing_mode is a feature flag, not a secret. Not consumed by any code yet in Phase 2A — automatic mode branching (validating -> caption_ready without human approval) is a future phase. Reserved here so enabling it later does not require a schema change."
}
